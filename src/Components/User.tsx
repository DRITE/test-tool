import * as React from 'react';
import {IProps} from './TestTaskButton';

export const User: React.SFC<IProps> = (props: IProps) => {
    return (
        <div>
            <p>Welcome on USer page!!</p>
        </div>
    )
}